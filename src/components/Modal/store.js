import { store as calendar } from './Calendar';
import { store as confirmation } from './Confirmation';
import { store as error } from './Error';
import { store as spinner } from './Spinner';
import { store as success } from './Success';

export default { 
  calendar,
  confirmation,
  error,
  spinner,
  success
}